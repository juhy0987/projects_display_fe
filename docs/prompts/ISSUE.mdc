**이슈를 기반으로 작업할 때에만 적용할 프롬프트**
다음 제공된 이슈 내용을 바탕으로 실제 구현 코드를 작성해줘.

[이슈 내용]
- gh 툴을 사용해 직접 접근해서 불러와줘

[요청 사항]
- **`.claude/rules/` 의 React 컨벤션, API 호출 규약, 브랜치 / 커밋 / PR 규약을 확인하고 준수해서 구현해.**
- 기존 레이어 구조 (pages / components / hooks / api) 를 따른다.
- HTTP 호출은 반드시 `src/api/` 를 거치도록 작성한다 (컴포넌트에서 `fetch` 직접 호출 금지).
- 새 기능에는 Vitest + Testing Library 기반 테스트를 함께 포함해줘.
- 모든 설명은 공식 문서나 신뢰할 수 있는 개발 가이드를 출처로 하여 작성해줘.
- 커밋은 논리적 변경 단위(TODO)마다 진행해줘.
- Agent 작업 흔적은 남기지 마.
